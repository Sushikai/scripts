[pytest]
markers =
    unit: 纯单元测试,不依赖 server/网络
    contract: API 契约测试,需 server 运行
    visual: 视觉/规则化扫描,不依赖 server
    e2e: 端到端,需 Playwright + server
    slow: 跑得久(>30s)

testpaths = tests
addopts = -ra --tb=short
filterwarnings =
    ignore::DeprecationWarning
